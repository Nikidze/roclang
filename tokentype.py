from enum import Enum
class TokenType(Enum):
    [NUMBER,
     DBLNUMBER,
     WORD,
     TEXT,
     PRINT,
     INPUT,
     PLUS,
     PLUSPLUS,
     PLUSEQ,
     MINUS,
     MINUSMINUS,
     MINUSEQ,
     STAR,
     STAREQ,
     SLASH,
     SLASHEQ,
     EQ,
     EQEQ,
     EXCL,
     EXCLEQ,
     LT,
     LTEQ,
     GT,
     GTEQ,
     IF,
     ELSE,
     WHILE,
     FOR,
     DO,
     BREAK,
     CONTINUE,
     RETURN,
     THREADS,
     SWITCH,
     SWITCHBREAK,
     DEF,
     BAR,
     BARBAR,
     AMP,
     AMPAMP,
     LPAREN,
     RPAREN,
     LBRACE,
     RBRACE,
     SEMICOLON,
     COLON,
     COMMA,
     TYPE,
     IMPORT,
     EOF,
     TAG,
     LSQBR,
     RSQBR,
     POINT,
     DBPOINT,
     TRPOINT,
     NEWSTR,
     END,
     VARTYPE,
     TAB] \
        = range(60)

